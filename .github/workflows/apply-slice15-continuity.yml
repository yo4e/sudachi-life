name: Apply Slice 15 continuity

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull-request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Apply exact continuity replacements
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file_path = Path(path)
              text = file_path.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one match, found {count}")
              file_path.write_text(text.replace(old, new), encoding="utf-8")

          replace_once(
              "AGENTS.md",
              "Slices 1–14 establish:",
              "Slices 1–15 establish:",
          )
          replace_once(
              "AGENTS.md",
              """- later normal-wake rejection without clock reads after retention failure
- the complete canonical four-wake run plus protected blocked-state, recovery, action-failure, budget-exhaustion, maintenance-threshold, maintenance-inspection, maintenance-clear, checkpoint-retention, and retention-failure fixtures

After PR #28 is merged, the exact next implementation slice is **Slice 15: explicit pending-checkpoint orphan registration repair**.

It must start from the protected checkpoint-deadline fixture after the committed boundary and immutable artifact exist but before registration, acquire fail-fast administrative ownership, identify exactly one valid published orphan matching the pending lineage and event boundary, fully validate it, atomically register it as latest stable, clear pending state, append typed repair audit history, preserve canonical lifecycle and inbox state, and restore normal wakeability.

Do not add ambiguous-orphan cleanup, checkpoint deletion, retention-failure maintenance clear, lineage rollback, a caregiver, chat, learning, memories, skills, or a general agent loop in Slice 15.
""",
              """- later normal-wake rejection without clock reads after retention failure
- explicit administrative `checkpoint repair-pending` API and CLI boundaries
- fail-fast pending-checkpoint repair ownership without input claim or environment advancement
- exact one-candidate validation against canonical identity, lineage, lifecycle, pending boundary, versions, protected configuration, database digest, manifest digest, and committed snapshot contents
- typed zero-candidate, ambiguous-candidate, foreign-organism, invalid-artifact, repeated-repair, and busy rejection without clock reads or pending-state changes
- atomic orphan registration, latest-stable advancement, pending-state clearing, return to `sleeping`, and typed `checkpoint_registration_repaired` audit history
- preservation of committed lifecycle state, inbox history, previous checkpoint, immutable artifacts, and checkpoint-store bytes through repair
- later canonical harvest processing at boundary 24 after the repaired boundary 13 becomes stable
- the complete canonical four-wake run plus protected blocked-state, recovery, action-failure, budget-exhaustion, maintenance-threshold, maintenance-inspection, maintenance-clear, checkpoint-retention, retention-failure, and pending-checkpoint-repair fixtures

After PR #29 is merged, the exact next implementation slice is **Slice 16: deterministic non-canonical JSONL event export**.

It must export one declared stable committed boundary through an explicit administrative read-only API and narrow CLI command, order records by canonical event sequence, identify organism, lineage, schema, contract, and event boundaries, produce byte-identical output for unchanged canonical state, write through a temporary file and atomic publication, and prove that export creation, deletion, modification, or failure cannot alter canonical SQLite state.

Do not add JSONL import, lifecycle dual-writing, organism-controlled export, rollback, orphan deletion, a caregiver, chat, learning, memories, skills, or a general agent loop in Slice 16.
""",
          )

          replace_once(
              "docs/HANDOFF.md",
              """Checkpoint repair, orphan cleanup, maintenance clear for the new reason, and rollback are not implemented.

## Verified implementation results
""",
              """Checkpoint repair, orphan cleanup, maintenance clear for the new reason, and rollback are not implemented.

### Slice 15 — explicit pending-checkpoint registration repair

Implemented and verified in PR #29:

- the protected first-water fixture commits boundary 13 and publishes its immutable lifecycle checkpoint artifact
- the checkpoint deadline expires one nanosecond beyond the protected 5,000 ms limit before registry insertion, leaving canonical status `checkpoint_pending`, event count 13, and latest stable boundary 2
- `repair_pending_checkpoint_registration` and `sudachi checkpoint repair-pending` expose one explicit administrative repair boundary
- the operation acquires fail-fast `BEGIN IMMEDIATE` ownership without claiming input or advancing the environment
- before any clock read, it requires exact pending identity and boundary, validates the previous stable checkpoint, rejects hidden or unsafe entries, requires exactly one visible unregistered artifact, and validates its identifier, lineage, lifecycle, versions, provenance, digests, protected configuration, and complete committed snapshot contents
- zero, multiple, foreign-organism, corrupted, repeated, and busy repair attempts reject without clearing pending state or reading the clock
- one injected administrative clock read supplies the successful registration and audit timestamp
- one transaction inserts the registry row, advances latest stable to boundary 13, clears pending fields, restores `sleeping`, and appends typed `checkpoint_registration_repaired` event 14
- no checkpoint artifact is copied, renamed, modified, or deleted; committed lifecycle state, environment, inbox history, genesis, and checkpoint-store bytes remain unchanged
- a later second tick runs under the unchanged fixed policy, harvests `bed-b`, completes the objective, stabilizes boundary 24 at event 25, and returns to `sleeping`

Ambiguous-orphan cleanup, checkpoint deletion, retention-failure maintenance clear, broad repair, and rollback are not implemented.

## Verified implementation results
""",
          )
          replace_once(
              "docs/HANDOFF.md",
              """GitHub Actions passed on PR #28 with Python 3.12:

- clean editable installation
- `python -m compileall -q src tests`
- **43 protected tests**
- installed genesis CLI smoke test
- all earlier canonical water, harvest, abstention, blocked-state, recovery, action-failure, and budget-exhaustion behavior remains protected
- the maintenance-threshold fixture proves exact failure transition, unchanged environment, checkpoint stabilization, typed maintenance entry, and later normal-wake rejection
- the maintenance-inspection fixture proves exact reporting, read-only files and canonical rows, typed non-maintenance rejection, and continued normal-wake blocking
- the maintenance-clear fixture proves bounded reason validation, fail-fast ownership, atomic state and audit commit, exact preservation, rollback on audit failure, and later processing of the preserved tick
- the checkpoint-retention fixture proves no pruning at four, fifth-checkpoint stability before pruning, genesis and latest preservation, oldest-eligible selection, exact artifact and registry removal, explicit byte-accounted audit history, and continued normal wakeability
- the retention-failure fixture proves fifth-checkpoint stability before failure, staged-artifact restoration, five-row/five-artifact preservation, exact latest references, no false pruning success, typed maintenance warning, read-only inspection, and later zero-clock wake rejection

The exact local source-tree run also passed **43 tests** and compileall. A separate local clean editable install could not resolve `hatchling>=1.25` from the execution environment's package mirror; GitHub Actions independently completed the clean install, so that mirror failure is not treated as success.
""",
              """GitHub Actions passed on PR #29 with Python 3.12:

- clean editable installation
- `python -m compileall -q src tests`
- **50 protected tests**
- installed genesis CLI smoke test
- all earlier canonical water, harvest, abstention, blocked-state, recovery, action-failure, and budget-exhaustion behavior remains protected
- the maintenance-threshold fixture proves exact failure transition, unchanged environment, checkpoint stabilization, typed maintenance entry, and later normal-wake rejection
- the maintenance-inspection fixture proves exact reporting, read-only files and canonical rows, typed non-maintenance rejection, and continued normal-wake blocking
- the maintenance-clear fixture proves bounded reason validation, fail-fast ownership, atomic state and audit commit, exact preservation, rollback on audit failure, and later processing of the preserved tick
- the checkpoint-retention fixture proves no pruning at four, fifth-checkpoint stability before pruning, genesis and latest preservation, oldest-eligible selection, exact artifact and registry removal, explicit byte-accounted audit history, and continued normal wakeability
- the retention-failure fixture proves fifth-checkpoint stability before failure, staged-artifact restoration, five-row/five-artifact preservation, exact latest references, no false pruning success, typed maintenance warning, read-only inspection, and later zero-clock wake rejection
- the pending-checkpoint-repair fixture proves exact one-orphan matching, full snapshot validation, zero-clock rejection for missing, ambiguous, foreign, invalid, repeated, and busy cases, atomic registry/pending/audit repair, artifact preservation, and later normal wakeability

The exact local source-tree run also passed **50 tests** and compileall. A separate local clean editable install could not resolve `hatchling>=1.25` from the execution environment's package mirror; GitHub Actions independently completed the clean install, so that mirror failure is not treated as success.
""",
          )
          replace_once(
              "docs/HANDOFF.md",
              """- later ordinary wake rejection consumes zero clock readings

## Integration repair record
""",
              """- later ordinary wake rejection consumes zero clock readings

The isolated Slice 15 pending-checkpoint-repair fixture immediately after repair has:

- `lifecycle_number = 1`
- `environment_step = 1`
- `bed-a.moisture = 1`
- `bed-b.fruit = 1`
- `water_units = 0`
- `harvested_fruit = 0`
- `objective_complete = false`
- `consecutive_failures = 0`
- `latest_stable_event_sequence = 13`
- `event_count = 14`
- `status = sleeping`
- registered checkpoint boundaries 2 and 13
- event 14 typed administrative `checkpoint_registration_repaired`
- checkpoint artifacts and exact checkpoint-store bytes unchanged

Before repair, the same fixture is committed at boundary 13 with event count 13, status `checkpoint_pending`, latest stable boundary 2, and exactly one valid published but unregistered boundary-13 artifact. Missing, ambiguous, foreign, invalid, repeated, and busy attempts leave that state unchanged with zero clock reads.

After a later second tick, the repaired fixture finishes with lifecycle two, environment step two, harvested fruit one, completed objective, stable boundary 24, event count 25, status `sleeping`, and both ticks consumed.

## Integration repair record
""",
          )
          replace_once(
              "docs/HANDOFF.md",
              """After PR #28 is merged, create a new branch from current `main` and implement **Slice 15: explicit pending-checkpoint orphan registration repair**.

The slice must:

1. start from the protected checkpoint-deadline fixture after a committed lifecycle boundary remains `checkpoint_pending`
2. require the immutable checkpoint artifact to have been published but not registered
3. expose one explicit administrative repair API and narrow CLI command, not an organism action
4. acquire fail-fast SQLite write ownership without claiming input or advancing the environment
5. locate exactly one published orphan whose organism identity, lineage generation, pending event boundary, versions, digest, and protected configuration match canonical pending state
6. fully validate the artifact and reject zero, multiple, mismatched, or invalid candidates without clearing pending state
7. atomically register the valid orphan as latest stable, clear pending state, restore `sleeping`, and append typed administrative repair history
8. preserve committed lifecycle state, event boundary, inbox rows, previous stable checkpoint, and artifact byte accounting
9. prove a later ordinary wake can proceed from the repaired stable state
10. pass protected tests and CI

Do not add ambiguous-orphan cleanup, checkpoint deletion, retention-failure maintenance clear, lineage rollback, caregiver consultation, learning, or generic planning in Slice 15.
""",
              """After PR #29 is merged, create a new branch from current `main` and implement **Slice 16: deterministic non-canonical JSONL event export**.

The slice must:

1. start from one declared stable committed organism boundary with no pending checkpoint
2. expose an explicit administrative read-only export API and narrow CLI command, not an organism action
3. open canonical SQLite read-only and declare the exact source lineage and event boundary
4. emit deterministic JSONL ordered by canonical event sequence
5. identify export format, organism, lineage, schema, contract, and first and last event boundaries
6. use canonical JSON serialization with no wall-clock-dependent output
7. write to a bounded temporary file and publish atomically only after complete validation
8. produce byte-identical output when canonical state is unchanged
9. prove export creation, deletion, modification, and injected write failure cannot alter canonical SQLite state, checkpoint state, inbox state, or wakeability
10. pass protected tests and CI

Do not add JSONL import, lifecycle dual-writing, organism-controlled export, rollback, orphan deletion, caregiver consultation, learning, or generic planning in Slice 16.
""",
          )

          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Status: **Slices 1–14 implemented — Phase 1 incomplete**",
              "Status: **Slices 1–15 implemented — Phase 1 incomplete**",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "`tests/test_clock.py::test_unexpected_clock_read_fails`; successful wakes consume five declared readings including the pre-action deadline check; the classified exhausted wake consumes four; protected maintenance inspection has no clock parameter and its later rejected wake consumes zero",
              "`tests/test_clock.py::test_unexpected_clock_read_fails`; successful wakes consume five declared readings including the pre-action deadline check; the classified exhausted wake consumes four; protected maintenance inspection has no clock parameter and its later rejected wake consumes zero; successful pending-checkpoint repair consumes one declared administrative read after validation, while every rejected repair consumes zero",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Genesis transaction, rollback-only wake preparation, and committed classified action-failure, budget-exhaustion, and maintenance-threshold lifecycles are protected; `tests/test_maintenance_clear.py::test_maintenance_clear_rolls_back_when_audit_event_fails` proves maintenance state and audit event commit atomically",
              "Genesis transaction, rollback-only wake preparation, and committed classified action-failure, budget-exhaustion, and maintenance-threshold lifecycles are protected; `tests/test_maintenance_clear.py::test_maintenance_clear_rolls_back_when_audit_event_fails` proves maintenance state and audit event commit atomically; `tests/test_pending_checkpoint_repair.py::test_valid_published_orphan_repairs_pending_registration_and_allows_next_wake` proves checkpoint registry insertion, latest-stable advancement, pending clear, sleeping restoration, and repair audit history commit atomically",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Genesis, all four canonical lifecycle sequences, blocked-state, recovery, classified action-failure, classified budget-exhaustion, maintenance-threshold, administrative maintenance-clear, checkpoint-pruning, and retention-failure-warning sequences are asserted",
              "Genesis, all four canonical lifecycle sequences, blocked-state, recovery, classified action-failure, classified budget-exhaustion, maintenance-threshold, administrative maintenance-clear, checkpoint-pruning, retention-failure-warning, and pending-checkpoint-registration-repair sequences are asserted",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Pending status is preserved by timeout test; explicit second-wake rejection remains planned",
              "Pending status is preserved by the timeout test; pending-checkpoint repair rejects zero, multiple, foreign, invalid, repeated, and busy cases without clearing pending state; explicit second-wake rejection before repair remains planned",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Genesis plus water, harvest, completion-abstention, blocked-abstention, recovery, action-failure, budget-exhaustion, and maintenance-threshold checkpoint validation",
              "Genesis plus water, harvest, completion-abstention, blocked-abstention, recovery, action-failure, budget-exhaustion, and maintenance-threshold checkpoint validation; pending repair additionally matches the unregistered candidate's canonical identifier, organism, lineage, lifecycle, versions, boundary, provenance, digests, protected configuration, and complete committed snapshot contents",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "`tests/test_checkpoint_timeout.py::test_checkpoint_timeout_preserves_committed_pending_boundary`",
              "`tests/test_checkpoint_timeout.py::test_checkpoint_timeout_preserves_committed_pending_boundary`; `tests/test_pending_checkpoint_repair.py::test_valid_published_orphan_repairs_pending_registration_and_allows_next_wake` proves exact administrative registration repair of the preserved published artifact",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Both protected action definitions and independent evaluators are used; maintenance inspection and clear are explicit administrative API/CLI boundaries; clear requires bounded caller reason, exact protected maintenance state, and fail-fast ownership; broader authority tests remain planned",
              "Both protected action definitions and independent evaluators are used; maintenance inspection, clear, and pending-checkpoint repair are explicit administrative API/CLI boundaries; repair requires exact pending state, fail-fast ownership, exactly one fully matching immutable candidate, and rejects before clock use when validation fails; broader authority tests remain planned",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "Input and checkpoint events use administrative sources; lifecycle events use the organism fixed-policy source; maintenance inspection is a separate read-only administrative command; maintenance clear records typed `maintenance_cleared` from source `administration:maintenance-clear`; retention records typed `checkpoint_pruned` or `checkpoint_retention_failed` from source `administration:checkpoint-retention`",
              "Input and checkpoint events use administrative sources; lifecycle events use the organism fixed-policy source; maintenance inspection is a separate read-only administrative command; maintenance clear records typed `maintenance_cleared` from source `administration:maintenance-clear`; retention records typed `checkpoint_pruned` or `checkpoint_retention_failed` from source `administration:checkpoint-retention`; pending repair records typed `checkpoint_registration_repaired` from source `administration:checkpoint-repair`",
          )
          replace_once(
              "docs/PHASE1_TEST_MATRIX.md",
              "PR #28 passed GitHub Actions on Python 3.12 with **43 protected tests**.",
              "PR #29 passed GitHub Actions on Python 3.12 with **50 protected tests**.",
          )
          PY
          rm .github/workflows/apply-slice15-continuity.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: advance continuity after Slice 15"
          git push
