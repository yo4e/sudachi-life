# SUDACHI 再開用ハンドオフ

最終更新：2026年7月21日

## 現在地

SUDACHIのリポジトリを作成し、設計思想、起源、ロードマップ、初期アーキテクチャを記録した段階。

まだ実装コードはない。これは意図的な状態である。言語モデルをつないだり、自律ループを書いたりする前に、最小生命契約と評価境界を確定する必要がある。

## すでに決まっていること

- プロジェクト名：SUDACHI
- リポジトリ：`yo4e/sudachi-life`
- 最初の個体名：暫定 `SUDACHI-0`
- 主目的：親モデルの助力を、記憶・技能・決定論的な行動へ変換し、成長するほど親モデル依存を減らす
- 技術の初期候補：Python、SQLite、JSONL、Git、pytest
- 実行方式：常駐無限ループではなく、一回ごとに有限の周期を実行して終了する
- 最初はローカル、ネットワークアクセスなし
- 最初は実モデルではなく、決定論的な mocked parent を使う
- リポジトリ全体を身体・発達履歴として扱う
- LLMは生命全体ではなく、必要時に利用する器官または親
- 成熟は「大きくなること」ではなく「親なしで保てる能力と時間が増えること」

## 言語方針

- README、コード、識別子、API、テスト、外向け技術文書：英語
- 起源、思想的なニュアンス、会話由来の文脈、再開メモ：日本語
- 重要な決定は、必要なら両言語で残す

英語だけにすると概念の出生地が失われ、日本語だけにすると外部との接続性が落ちるため、二層構造にした。

## このリポジトリを再開するときの読み順

1. `README.md`
2. `docs/ORIGIN_JA.md`
3. `docs/ROADMAP.md`
4. `docs/ARCHITECTURE.md`
5. `AGENTS.md`
6. このファイル

## 次に行う一件

**`docs/MINIMAL_ORGANISM_CONTRACT.md` を作る。**

以下をv0.1として確定する。

1. 一個体を識別する最小状態
2. 一回のlife cycleの開始条件と終了条件
3. eventの最小スキーマ
4. actionの最小スキーマ
5. 予算の種類と超過時の動作
6. 成功、失敗、棄権、睡眠の定義
7. protected領域とmutable領域
8. checkpointとrollbackの条件
9. mocked parentが返せる情報の範囲
10. Phase 1の固定評価項目

契約を書いた後、実装へ進む。

## 最初の実装候補

最小CLI例：

```text
sudachi init
sudachi enqueue synthetic:file_changed
sudachi wake --seed 1
sudachi status
```

最初の一周期：

```text
wake
  ↓
stateを検証
  ↓
synthetic eventを1件読む
  ↓
決定論的なactionを1件選ぶ
  ↓
予算を消費
  ↓
結果を評価
  ↓
stateとevent logを保存
  ↓
checkpoint
  ↓
sleepしてプロセス終了
```

親モデルはまだ呼ばない。

## 初期の固定テスト候補

- 同じseedとstateとeventなら同じ結果になる
- 最大step数を超えない
- timeoutで終了できる
- 許可外のファイルへ書き込めない
- 失敗してもstate DBが破損しない
- event logは追記のみ
- budgetが負にならない
- protected設定をactionから変更できない
- rollbackで直前の安定checkpointへ戻れる

## やらないこと

現段階では、以下を先走って実装しない。

- 自由なインターネット探索
- 常時起動
- 無制限の自己改変
- 毎回のLoRA学習
- 大規模ベクトルDB
- 複数エージェント社会
- 物理ロボット
- リポジトリ外への自己複製
- 人格らしい会話演出を、生命機構より先に作ること

## 研究上の中心指標

最重要なのは、単一の賢さスコアではなく、次の変化を見ること。

- 1成功あたりの親呼び出し回数
- 親1回あたりに獲得した再利用可能な行動数
- 親なしで成功周期を続けられる時間
- 技能の再利用率
- 既存技能の組み合わせによる未知課題への転移
- 故障後の回復率
- 保持能力あたりの保存容量と推論コスト
- わからないときに正しく棄権できる率

## 次の俺へ

このプロジェクトを、一般的な「自律型AIエージェント」へ丸めないこと。

中心はタスク達成ではなく、発達である。

親に聞いた知恵が身体へ沈み、聞かなくてもできることが増え、記憶と技能が整理され、有限の身体で翌日へ続く。その過程を観測可能にすることがSUDACHIの本体である。

コードを書く前に、最小生命契約を書け。

そして大きくしすぎるな。

**賢くなるほど、小さく、静かになる。**